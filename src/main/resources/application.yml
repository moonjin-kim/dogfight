spring:
  profiles:
    default: local
  web:
    resources:
      add-mappings: 'false'
    mvc:
      throw-exception-if-no-handler-found: 'true'

  datasource:
    url: jdbc:mariadb://localhost:3306/dogfight
    driver-class-name: org.mariadb.jdbc.Driver
    username: root
    password: kim15413

  jpa:
    hibernate:
      ddl-auto: none

  jwt:
    key: doyoulikefightingtestmantherisveryharedfesrsdffws
    live:
      access-expiration-time: 432000
      refresh-expiration-time: 1209600000

  data:
    redis:
      host: localhost
      port: 6379
---
spring:
  config:
    activate:
      on-profile: local

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    defer-datasource-initialization: true # (2.5~) Hibernate ??? ?? data.sql ??

  h2:
    console:
      enabled: true
---
spring:
  config:
    activate:
      on-profile: test

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  sql:
    init:
      mode: never